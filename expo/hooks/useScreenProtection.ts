/**
 * Screenshot protection is intentionally disabled so app screens can be captured
 * for testing, support, and App Store screenshot workflows.
 */
export function useScreenProtection(_enabled: boolean, _key: string): void {
  return;
}
