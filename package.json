{
  "name": "rork-amen-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "bun --cwd expo run start",
    "web": "bun --cwd expo run start-web",
    "test": "bun --cwd expo test",
    "lint": "bun --cwd expo run lint",
    "postinstall": "bun install --cwd expo"
  }
}
